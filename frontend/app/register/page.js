import RegisterForm from '../../src/features/auth/components/RegisterForm';

export const metadata = {
  title: 'Criar Conta — Momentum',
  description: 'Crie sua conta no Momentum para começar a acompanhar sua rotina, metas e produtividade.',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
